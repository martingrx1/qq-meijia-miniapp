<!-- miniprogram/pages/navigation/index.wxml -->
<view style="margin:0 26px">
    <button class="nav" wx:for="{{nav}}">
        <navigator url="{{item.path}}">{{item.title}}</navigator>
    </button>
</view>