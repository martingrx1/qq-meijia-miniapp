function isSub(customers,_openid){
  console.log(_openid,'openid')
  if(!customers) return false;
  for(var i = 0; i<customers.length;i++){
    if(customers[i]._openid === _openid) return true
  }
  return false;

}

module.exports = {
  isSub : isSub
}